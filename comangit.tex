git add .
git commit -m "primeiro commit"
git remote add origin https://github.com/sanctiag/Fullstck_Senai.git
git branch -M main
git push -u origin main
echo "node_modules" >> .gitignore
git rm -r --cached node_modules | >> remoção do node_modules
